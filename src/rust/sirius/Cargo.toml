[package]
name = "sirius"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
nmea = { version = "0.7.0", features = ["default"]}
serialport = { version = "4.7.3", default-features = false }
